function UppedMatrix=UpsampleMatrix(InputMatrix)%18.086 Final Project%By Joseph Kovac%Revised and submitted for OCW 9/12/05%Take padded matrix of error solution to residual problem, give back upsampled zero padded errorInteriorEdge=sqrt(prod(size(InputMatrix)))-2;%Create the upsampled by transposing the downsampling matrix and scalingE=RemoveBoundaries(InputMatrix(1:end)');SmallDowner=CreateCoarsener(InteriorEdge*2+1,InteriorEdge*2+1);eUpsampled=4*SmallDowner'*E(1:end);PaddedError=AddResidualZeros(WrapVector(eUpsampled));UppedMatrix=PaddedError;